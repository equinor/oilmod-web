import { StoGridDirective } from './sto-grid.directive';

describe('StoGridDirective', () => {
  it('should create an instance', () => {
    const directive = new StoGridDirective();
    expect(directive).toBeTruthy();
  });
});
